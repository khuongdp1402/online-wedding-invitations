export default function TemplatePickerPage({
  params: _params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-900 mb-8">
        Chọn Mẫu Thiệp
      </h1>
      <p className="text-gray-500">Template picker - Phase 2</p>
    </div>
  );
}
